import { FiltroCandidatoPipe } from './filtro-candidato.pipe';

describe('FiltroCandidatoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroCandidatoPipe();
    expect(pipe).toBeTruthy();
  });
});
